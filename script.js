const menu=document.querySelector('#menu'),links=document.querySelector('#navlinks'),header=document.querySelector('.nav');
if(menu&&links){menu.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.classList.toggle('open',open);menu.setAttribute('aria-expanded',open)});document.querySelectorAll('#navlinks a').forEach(a=>a.addEventListener('click',()=>{links.classList.remove('open');menu.classList.remove('open')}))}
addEventListener('scroll',()=>header?.classList.toggle('scrolled',scrollY>60));
const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');revealObserver.unobserve(entry.target)}}),{threshold:.13});document.querySelectorAll('.reveal').forEach(section=>revealObserver.observe(section));
document.querySelectorAll('.project-filters button').forEach(filter=>filter.addEventListener('click',()=>{document.querySelectorAll('.project-filters button').forEach(button=>button.classList.remove('selected'));filter.classList.add('selected');const category=filter.dataset.filter;document.querySelectorAll('.project-item').forEach(item=>item.classList.toggle('hidden',category!=='all'&&item.dataset.category!==category))}));
const form=document.querySelector('#contact-form');if(form)form.addEventListener('submit',event=>{event.preventDefault();const message=document.querySelector('#form-message');message.textContent='Thank you. Your enquiry is ready to send - please connect this form to your preferred email service before launch.';form.reset()});
